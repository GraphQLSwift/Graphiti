query($sku: String!, $package: String!) {
    deprecatedProduct(sku: $sku, package: $package) {
        sku
        package
        reason
        createdBy {
            email
            name
            totalProductsCreated
            yearsOfEmployment
            averageProductsCreatedPerYear
        }
    }
}
